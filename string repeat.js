//write a function called repeartStr wich repearts the given string exactly n times 
//escribre una funcion llamada repeatStr que repita el string dado exactamente n veces

// Dividimos tareas
// 1. Creamos la función
// 2. Usamos el método repeat para repetir el string
// 3. Retornamos el string

function repeartStr(num, str) {
        return str.repeat(num);
    }

    repeartStr(6, " jordan ")

    /*

    Situación: Necesitabas repetir una cadena n veces.

    Tarea: Crear una función que recibiera el número de repeticiones y la cadena, y devolviera la cadena repetida.

    Acción: Utilizaste el método repeat dentro de una función para lograrlo.

    Resultado: La función devuelve la cadena repetida correctamente.
    
    */