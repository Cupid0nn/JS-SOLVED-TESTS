//make a function that takes strings and convert into a number
//crea una funcion que tome strings y convierta en un number

function toString(string) { 
    return Number(string);; // tomamos el numero y lo devolvemos a string con toString
}

console.log(toString('123')); // 123
console.log(toString('999'));// 999